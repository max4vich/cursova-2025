# E-Commerce Platform - Курсова робота 2025

Повнофункціональна платформа електронної комерції з адмін-панеллю, системою замовлень, промокодами та звітністю.

## 📋 Зміст

- [Технології](#технології)
- [Вимоги](#вимоги)
- [Структура проекту](#структура-проекту)
- [Встановлення](#встановлення)
- [Налаштування бази даних](#налаштування-бази-даних)
- [Налаштування змінних оточення](#налаштування-змінних-оточення)
- [Запуск проекту](#запуск-проекту)
- [Доступ до додатку](#доступ-до-додатку)
- [Розробка](#розробка)
- [Розгортання](#розгортання)
- [Усунення проблем](#усунення-проблем)

## 🛠 Технології

### Backend
- **Node.js** - серверна платформа
- **Express.js** - веб-фреймворк
- **Prisma ORM** - ORM для роботи з базою даних
- **MySQL** - реляційна база даних
- **JWT** - автентифікація та авторизація
- **bcryptjs** - хешування паролів
- **Multer** - завантаження файлів

### Frontend
- **React 19** - UI бібліотека
- **React Router** - маршрутизація
- **React Context API** - управління станом
- **React Query** - кешування та синхронізація даних

## 📦 Вимоги

Перед початком роботи переконайтеся, що у вас встановлено:

- **Node.js** версії 18 або вище ([завантажити](https://nodejs.org/))
- **npm** або **yarn** (встановлюється разом з Node.js)
- **MySQL** версії 8.0 або вище ([завантажити](https://dev.mysql.com/downloads/mysql/))
- **Git** ([завантажити](https://git-scm.com/))

### Перевірка встановлення

```bash
node --version    # Має бути v18.0.0 або вище
npm --version     # Має бути 9.0.0 або вище
mysql --version   # Має бути 8.0.0 або вище
```

## 📁 Структура проекту

```
cursova-2025/
├── backend/              # Backend сервер (Node.js/Express)
│   ├── src/             # Вихідний код
│   │   ├── controllers/ # Контролери
│   │   ├── services/    # Бізнес-логіка
│   │   ├── routes/      # API маршрути
│   │   ├── middleware/  # Middleware
│   │   ├── patterns/    # GoF патерни проектування
│   │   └── utils/       # Допоміжні функції
│   ├── prisma/          # Prisma схема та міграції
│   ├── uploads/         # Завантажені файли
│   └── package.json     # Залежності backend
├── frontend/            # Frontend додаток (React)
│   ├── src/             # Вихідний код
│   │   ├── components/  # React компоненти
│   │   ├── pages/       # Сторінки додатку
│   │   ├── contexts/    # React Context
│   │   └── api/         # API клієнт
│   └── package.json     # Залежності frontend
└── docs/                # Документація проекту
```

## 🚀 Встановлення

### 1. Клонування репозиторію

```bash
git clone <url-репозиторію>
cd cursova-2025
```

### 2. Встановлення залежностей Backend

```bash
cd backend
npm install
```

### 3. Встановлення залежностей Frontend

```bash
cd ../frontend
npm install
```

## 🗄 Налаштування бази даних

### 1. Створення бази даних MySQL

Увійдіть до MySQL:

```bash
mysql -u root -p
```

Створіть базу даних:

```sql
CREATE DATABASE cursova CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 2. Налаштування Prisma

Prisma автоматично згенерує клієнт під час встановлення залежностей. Якщо потрібно згенерувати вручну:

```bash
cd backend
npm run prisma:generate
```

## ⚙️ Налаштування змінних оточення

### Backend (.env файл)

Створіть файл `.env` в директорії `backend/` на основі `env.example`:

```bash
cd backend
cp env.example .env
```

Відредагуйте файл `.env` та встановіть наступні значення:

```env
PORT=5000
DATABASE_URL="mysql://username:password@localhost:3306/cursova"
JWT_SECRET="your-super-secure-secret-key-change-this-in-production"
JWT_EXPIRES_IN="7d"
ADMIN_EMAIL="admin@shop.com"
ADMIN_PASSWORD="ChangeMe123!"
```

**Важливо:**
- Замініть `username` та `password` на ваші MySQL облікові дані
- Змініть `JWT_SECRET` на випадковий безпековий ключ (мінімум 32 символи)
- Змініть `ADMIN_EMAIL` та `ADMIN_PASSWORD` на безпечні значення

### Frontend (.env файл)

Створіть файл `.env` в директорії `frontend/`:

```bash
cd frontend
```

Створіть файл `.env` з наступним вмістом:

```env
REACT_APP_API_URL=http://localhost:5000/api
```

Або якщо використовується змінна `API_BASE_URL`:

```env
API_BASE_URL=http://localhost:5000/api
```

**Примітка:** Якщо backend працює на іншому порту або домені, змініть URL відповідно.

## 🏃 Запуск проекту

### Розробка (Development)

#### 1. Запуск бази даних

Переконайтеся, що MySQL сервер запущений:

```bash
# Windows (якщо MySQL встановлено як служба, він запускається автоматично)
# Або через Services:
services.msc

# Linux/Mac
sudo systemctl start mysql
# або
brew services start mysql
```

#### 2. Застосування міграцій бази даних

```bash
cd backend
npm run prisma:migrate
```

Ця команда:
- Застосує всі міграції до бази даних
- Створить структуру таблиць
- Згенерує Prisma Client

#### 3. Заповнення бази даних тестовими даними (опціонально)

```bash
cd backend
npm run prisma:seed
```

Це створить тестові категорії, товари та інші дані для розробки.

#### 4. Запуск Backend сервера

Відкрийте перший термінал:

```bash
cd backend
npm run dev
```

Сервер запуститься на `http://localhost:5000` (або порт, вказаний в `.env`).

Ви побачите повідомлення:
```
API listening on port 5000
```

#### 5. Запуск Frontend додатку

Відкрийте другий термінал:

```bash
cd frontend
npm start
```

Додаток автоматично відкриється в браузері на `http://localhost:3000`.

### Продакшн (Production)

#### Backend

```bash
cd backend
npm run prisma:deploy  # Застосувати міграції
npm start              # Запустити сервер
```

#### Frontend

```bash
cd frontend
npm run build          # Зібрати production версію
```

Після збірки файли будуть в `frontend/build/`. Використовуйте веб-сервер (наприклад, nginx) для обслуговування статичних файлів.

## 🌐 Доступ до додатку

### Frontend (Користувацький інтерфейс)
- **URL:** http://localhost:3000
- **Функції:**
  - Перегляд каталогу товарів
  - Реєстрація та вхід
  - Додавання товарів до кошика
  - Оформлення замовлень
  - Профіль користувача

### Backend API
- **URL:** http://localhost:5000/api
- **Документація:** Перегляньте файли в `backend/src/routes/` для списку доступних ендпоінтів

### Адмін-панель
- **URL:** http://localhost:3000/admin
- **Вхід:** Використовуйте облікові дані з `ADMIN_EMAIL` та `ADMIN_PASSWORD` з файлу `.env`
- **Функції:**
  - Управління товарами
  - Управління категоріями
  - Управління замовленнями
  - Управління промокодами
  - Перегляд звітів
  - Управління користувачами

## 💻 Розробка

### Структура API

API використовує REST архітектуру:

- `GET /api/products` - отримати список товарів
- `GET /api/products/:id` - отримати товар за ID
- `POST /api/auth/register` - реєстрація
- `POST /api/auth/login` - вхід
- `GET /api/cart` - отримати кошик
- `POST /api/cart` - додати товар до кошика
- `POST /api/orders` - створити замовлення
- `GET /api/admin/*` - адмін ендпоінти (потрібна авторизація)

### GoF Патерни проектування

Проект використовує наступні патерни:

- **Facade** - `CheckoutFacade.js`, `frontend/src/api/client.js`
- **Builder** - `OrderBuilder.js`
- **Factory Method** - `PromotionFactory.js`
- **Strategy** - `ShippingStrategy.js`
- **Observer** - React Context (`AuthContext.js`, `CartContext.js`)

Детальна документація в `docs/design-patterns.md`.

### Створення нових міграцій

```bash
cd backend
npm run prisma:migrate
# Введіть назву міграції, наприклад: add_new_field
```

### Перегляд бази даних

Використовуйте Prisma Studio для візуального перегляду:

```bash
cd backend
npx prisma studio
```

Відкриється веб-інтерфейс на `http://localhost:5555`.

## 🚢 Розгортання

### Розгортання на Vercel (Backend)

1. Встановіть Vercel CLI:
```bash
npm i -g vercel
```

2. Увійдіть до Vercel:
```bash
vercel login
```

3. Розгорніть backend:
```bash
cd backend
vercel
```

4. Налаштуйте змінні оточення в Vercel Dashboard:
   - `DATABASE_URL`
   - `JWT_SECRET`
   - `ADMIN_EMAIL`
   - `ADMIN_PASSWORD`

5. Застосуйте міграції:
```bash
vercel env pull .env.production
npm run prisma:deploy
```

### Розгортання Frontend

1. Зберіть проект:
```bash
cd frontend
npm run build
```

2. Розгорніть `build/` директорію на вашому хостингу (Vercel, Netlify, або інший).

3. Налаштуйте змінну оточення `REACT_APP_API_URL` на URL вашого backend API.

## 🔧 Усунення проблем

### Проблема: "Cannot connect to database"

**Рішення:**
1. Перевірте, що MySQL сервер запущений
2. Перевірте правильність `DATABASE_URL` в `.env`
3. Переконайтеся, що база даних `cursova` створена
4. Перевірте права доступу користувача MySQL

### Проблема: "Prisma Client not generated"

**Рішення:**
```bash
cd backend
npm run prisma:generate
```

### Проблема: "Port 5000 already in use"

**Рішення:**
1. Змініть `PORT` в `backend/.env` на інший порт (наприклад, 5001)
2. Оновіть `REACT_APP_API_URL` в `frontend/.env` відповідно

### Проблема: "CORS error" в браузері

**Рішення:**
1. Перевірте, що backend запущений
2. Перевірте правильність `REACT_APP_API_URL` в `frontend/.env`
3. Переконайтеся, що CORS налаштований в `backend/src/app.js`

### Проблема: "Migration failed"

**Рішення:**
1. Перевірте підключення до бази даних
2. Переконайтеся, що база даних порожня або міграції сумісні
3. Якщо потрібно скинути базу даних:
```bash
cd backend
npx prisma migrate reset
npm run prisma:seed
```

### Проблема: "Module not found"

**Рішення:**
```bash
# Backend
cd backend
rm -rf node_modules package-lock.json
npm install

# Frontend
cd frontend
rm -rf node_modules package-lock.json
npm install
```

## 📚 Додаткова документація

- [Архітектура системи](docs/architecture.mdc)
- [Діаграма класів](docs/class-diagram.md)
- [Патерни проектування](docs/design-patterns.md)
- [Потік замовлень](docs/order-flow.md)
- [Use Case діаграма](docs/use-case-diagram.md)

## 📝 Ліцензія

MIT

## 👤 Автор

Курсова робота 2025

---

**Примітка:** Для детальнішої інформації про API ендпоінти, перегляньте код в `backend/src/routes/` або використовуйте інструменти для тестування API (Postman, Insomnia).

